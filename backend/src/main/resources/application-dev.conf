database {
    driver = "org.postgresql.Driver"
    jdbcUrl = "jdbc:postgresql://localhost:5432/khdb"
    user = "kh"
    password = "Abc123456%"
    maximumPoolSize = 10
}

app {
    cors {
        anyHost = true
    }

    rateLimit {
        requestsPerMinute = 60
    }
}