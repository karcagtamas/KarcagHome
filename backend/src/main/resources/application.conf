ktor {
    environment = ${?APP_ENV}
    port = ${?PORT}
}

app {
    cors {
        anyHost = true
    }

    rateLimit {
        requestsPerMinute = 60
    }
}

database {
    driver = "org.postgresql.Driver"
    jdbcUrl = "jdbc:postgresql://localhost:5432/khdb"
    user = "kh"
    password = "Abc123456%"
    maxPoolSize = 10
}