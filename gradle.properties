# Give the Kotlin Compiler enough RAM to handle Wasm IR generation
kotlin.daemon.jvmargs=-Xmx4G

# Give Gradle itself some breathing room
org.gradle.jvmargs=-Xmx2G

# Optional: Enable caching to speed up future builds
org.gradle.caching=true
org.gradle.parallel=true